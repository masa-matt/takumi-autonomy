services:
  takumi-bot:
    build: .
    image: takumi-bot:latest
    container_name: takumi-bot
    restart: unless-stopped

    env_file:
      - .env

    # runtime/ をホストにマウント — 再起動してもメモリ・レポートが残る
    # inbox/ をホストにマウント — ユーザーがファイルを置く場所（read-only）
    volumes:
      - ./runtime:/app/runtime
      - ./inbox:/app/inbox:ro

    # ログをローテーションする
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "5"
