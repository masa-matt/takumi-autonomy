# 実行結果

## タスク
hello.py を作って → hello.py が outbox に自動で出る

## 成果物
- `output/hello.py` — "Hello, World!" を出力するシンプルな Python スクリプト

## 動作確認
```bash
$ python output/hello.py
Hello, World!
```

`output/` ディレクトリが outbox として機能する前提で、`hello.py` をそこに配置しました。
